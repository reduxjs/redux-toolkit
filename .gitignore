.DS_Store
*.log
node_modules
# Dist and query are both build output folders
dist*/
# But don't ignore the RTK Query source
lib
es

.yalc
yalc.lock
yalc.sig

.idea/
.vscode/
temp/
.tmp-projections
build/
.rts2*
coverage/

typesversions
.cache
# The root is on pnpm. `.yarn/releases/yarn-4.4.1.cjs` is still tracked because
# the `examples/publish-ci/*` projects are still Yarn 4 and point their
# `yarnPath` at it.
.yarnrc
.yarn/*
!.yarn/releases
.pnp.*
*.tgz

tsconfig.vitest-temp.json

# node version manager files
.node-version
.nvmrc

# Assorted local docs
docs/dev-plans
# Perf protobufs
*.pb.gz