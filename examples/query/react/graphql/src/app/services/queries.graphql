query GetPosts($skip: Int = 0, $take: Int = 10) {
  posts(skip: $skip, take: $take) {
    id
    title
    status
  }
}

query GetPost($id: ID!) {
  post(where: { id: { equals: $id } }) {
    id
    title
    content
  }
}
