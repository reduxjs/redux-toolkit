type AllColors = {
  primary: string
  white: string
  lighter: string
  light: string
  dark: string
  darker: string
  black: string
}

/**
 * React Native 0.86 removed `react-native/Libraries/NewAppScreen`, which used
 * to export this palette. The values are copied from that module so the
 * example keeps its original appearance.
 */
export const TypedColors: AllColors = {
  primary: '#1292B4',
  white: '#FFF',
  lighter: '#F3F3F3',
  light: '#DAE1E7',
  dark: '#444',
  darker: '#222',
  black: '#000',
}
